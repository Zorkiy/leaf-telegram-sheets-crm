#!/bin/bash

# 1. Задаємо змінні (підставте свої реальні дані!)
export BOT_TOKEN=""
export WEBHOOK_URL=""
export SECRET_TOKEN=""

# 2. Виконуємо запит
curl -F "url=$WEBHOOK_URL" \
     -F "secret_token=$SECRET_TOKEN" \
     "https://api.telegram.org/bot$BOT_TOKEN/setWebhook"
